"""Runner version constant.

Bumping this string invalidates every cache entry. Use it to force re-runs
after a schema change in AgentRecord, BfclCase, or the predictions format.
"""
RUNNER_VERSION = "0.1.0"
